    =-- 加载通知
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "67 脚本",
    Text = "正在加载中...",
    Duration = 2
})
task.wait(2)

-- ========== 新增内容：通知内容 + 反挂机 + RevenantLib ==========
local bindable = Instance.new("BindableEvent")  -- 修复 bindable 未定义

game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "67脚本",
    Text = "每周都会更新几个服务器,更新的有点一慢,请见谅",
    Duration = 2,
    Callback = bindable,
})
wait(1.5)

local VirtualUserService = game:GetService("VirtualUser")
game:GetService("Players").LocalPlayer.Idled:connect(function()
    VirtualUserService:Button2Down(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
    wait(1)
    VirtualUserService:Button2Up(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
end)

game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "NH脚本",
    Text = "已开启反挂机",
    Duration = 2,
    Callback = bindable,
    
})

local RevenantLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/Revenant", true))()
RevenantLib.DefaultColor = Color3.fromRGB(255, 0, 0)
RevenantLib:Notification({
    Text = "67脚本作者:  勾勾搭\u{e000}",
    Duration = 3,
})
wait(1)
-- ========== 新增内容结束 ==========

-- ==============================================
-- AI Generated SimpleUI Library (白色背景版 · 子体黑色)
-- ==============================================
local Library = {}
local r78_1 = "Unknown"
if syn then r78_1 = "Synapse X"
elseif KRNL then r78_1 = "Krnl"
elseif fluxus then r78_1 = "Fluxus"
elseif getexecutorname then r78_1 = getexecutorname()
end

local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local LocalPlayer = game:GetService("Players").LocalPlayer
local Mouse = LocalPlayer:GetMouse()
local notifyQueue = {}
local isNotifying = false

local function ShowNotify(title, text)
    isNotifying = true
    local gui = game:GetService("CoreGui"):FindFirstChild("SimpleUI_PinkTheme")
    if not gui then return end

    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 260, 0, 70)
    frame.Position = UDim2.new(1, 270, 1, -80)
    frame.AnchorPoint = Vector2.new(1, 1)
    frame.BackgroundColor3 = Color3.new(0, 0, 0)
    frame.BackgroundTransparency = 0.1
    frame.Parent = gui

    local corner = Instance.new("UICorner", frame)
    corner.CornerRadius = UDim.new(0, 8)
    local stroke = Instance.new("UIStroke", frame)
    stroke.Color = Color3.fromRGB(200, 200, 200)
    stroke.Thickness = 1

    local titleLabel = Instance.new("TextLabel")
    titleLabel.Size = UDim2.new(1, -12, 0, 24)
    titleLabel.Position = UDim2.new(0, 8, 0, 6)
    titleLabel.BackgroundTransparency = 1
    titleLabel.Text = title
    titleLabel.TextColor3 = Color3.new(0, 0, 0)
    titleLabel.TextSize = 14
    titleLabel.Font = Enum.Font.GothamBold
    titleLabel.Parent = frame

    local textLabel = Instance.new("TextLabel")
    textLabel.Size = UDim2.new(1, -12, 0, 30)
    textLabel.Position = UDim2.new(0, 8, 0, 30)
    textLabel.BackgroundTransparency = 1
    textLabel.Text = text
    textLabel.TextColor3 = Color3.fromRGB(0, 120, 255)
    textLabel.TextSize = 12
    textLabel.Font = Enum.Font.Gotham
    textLabel.Parent = frame

    TweenService:Create(frame, TweenInfo.new(0.3), {Position = UDim2.new(1, -10, 1, -80)}):Play()
    task.wait(2.5)
    TweenService:Create(frame, TweenInfo.new(0.3), {Position = UDim2.new(1, 270, 1, -80)}):Play()
    task.wait(0.4)
    frame:Destroy()

    isNotifying = false
    if #notifyQueue > 0 then
        local n = table.remove(notifyQueue, 1)
        ShowNotify(n[1], n[2])
    end
end

function Library:Notify(title, text)
    if isNotifying then
        table.insert(notifyQueue, {title, text})
    else
        ShowNotify(title, text)
    end
end

-- ==============================================
-- 主UI库（白色背景 + 黑色子体）
-- ==============================================
function Library:CreateWindow(Config)
    local Title = Config.Title or "Script"
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "SimpleUI_PinkTheme"
    ScreenGui.ResetOnSpawn = false

    if syn and syn.protect_gui then
        syn.protect_gui(ScreenGui)
        ScreenGui.Parent = game:GetService("CoreGui")
    else
        ScreenGui.Parent = game:GetService("CoreGui")
    end

    -- ========== 打开按钮（白色背景黑字） ==========
    local OpenBtn = Instance.new("TextButton")
    OpenBtn.Name = "OpenButton"
    OpenBtn.Size = UDim2.new(0, 80, 0, 30)
    OpenBtn.Position = UDim2.new(0, 20, 0.5, -15)
    OpenBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    OpenBtn.Text = "打开"
    OpenBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
    OpenBtn.Font = Enum.Font.GothamBold
    OpenBtn.TextSize = 12
    OpenBtn.Visible = false
    OpenBtn.Parent = ScreenGui
    
    local OpenBtnCorner = Instance.new("UICorner", OpenBtn)
    OpenBtnCorner.CornerRadius = UDim.new(0, 6)
    local OpenBtnStroke = Instance.new("UIStroke", OpenBtn)
    OpenBtnStroke.Color = Color3.fromRGB(200, 200, 200)
    OpenBtnStroke.Thickness = 2

    -- 打开按钮拖动变量
    local openBtnDragging = false
    local openBtnDragInput = nil
    local openBtnDragStart = nil
    local openBtnStartPos = nil
    local openBtnDraggedRecently = false

    OpenBtn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            openBtnDragging = true
            openBtnDragStart = input.Position
            openBtnStartPos = OpenBtn.Position
            openBtnDraggedRecently = false
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    openBtnDragging = false
                end
            end)
        end
    end)

    OpenBtn.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            openBtnDragInput = input
        end
    end)

    local function updateOpenBtn(input)
        local delta = input.Position - openBtnDragStart
        if delta.Magnitude > 5 then
            openBtnDraggedRecently = true
        end
        OpenBtn.Position = UDim2.new(openBtnStartPos.X.Scale, openBtnStartPos.X.Offset + delta.X, openBtnStartPos.Y.Scale, openBtnStartPos.Y.Offset + delta.Y)
    end

    UserInputService.InputChanged:Connect(function(input)
        if input == openBtnDragInput and openBtnDragging then
            updateOpenBtn(input)
        end
    end)

    -- ========== 主窗口（白色背景） ==========
    local MainFrame = Instance.new("Frame")
    MainFrame.Name = "MainFrame"
    MainFrame.Size = UDim2.fromOffset(450, 300)
    MainFrame.Position = UDim2.fromScale(0.5, 0.5)
    MainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
    MainFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)   -- 纯白背景
    MainFrame.BorderSizePixel = 0
    MainFrame.Parent = ScreenGui

    -- 移除渐变，纯白背景
    local UICorner = Instance.new("UICorner", MainFrame)
    UICorner.CornerRadius = UDim.new(0, 8)
    local UIStroke = Instance.new("UIStroke", MainFrame)
    UIStroke.Color = Color3.fromRGB(200, 200, 200)
    UIStroke.Thickness = 2

    local function ToggleUI()
        local isOpened = MainFrame.Visible
        if isOpened then
            MainFrame.Visible = false
            OpenBtn.Visible = true
        else
            MainFrame.Visible = true
            OpenBtn.Visible = false
        end
    end

    OpenBtn.MouseButton1Click:Connect(function()
        if openBtnDraggedRecently then
            openBtnDraggedRecently = false
            return
        end
        ToggleUI()
    end)

    -- 标题栏（黑色文字）
    local TitleLabel = Instance.new("TextLabel")
    TitleLabel.Size = UDim2.new(1, -20, 0, 32)
    TitleLabel.Position = UDim2.new(0, 12, 0, 0)
    TitleLabel.BackgroundTransparency = 1
    TitleLabel.Text = Title
    TitleLabel.TextColor3 = Color3.fromRGB(0, 0, 0)      -- 黑色标题
    TitleLabel.TextSize = 15
    TitleLabel.Font = Enum.Font.GothamBold
    TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
    TitleLabel.Parent = MainFrame

    -- 关闭按钮（黑色）
    local CloseBtn = Instance.new("TextButton")
    CloseBtn.Size = UDim2.new(0, 26, 0, 26)
    CloseBtn.Position = UDim2.new(1, -30, 0, 4)
    CloseBtn.BackgroundTransparency = 1
    CloseBtn.Text = "X"
    CloseBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
    CloseBtn.TextSize = 14
    CloseBtn.Font = Enum.Font.Gotham
    CloseBtn.Parent = MainFrame
    CloseBtn.MouseButton1Click:Connect(ToggleUI)

    -- 主窗口拖动逻辑
    local dragging = false
    local dragInput = nil
    local dragStart = nil
    local startPos = nil

    local function update(input)
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end

    TitleLabel.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = MainFrame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)

    TitleLabel.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            update(input)
        end
    end)

    -- 左侧标签页容器（背景透明）
    local TabContainer = Instance.new("ScrollingFrame")
    TabContainer.Name = "TabContainer"
    TabContainer.Size = UDim2.new(0, 110, 1, -40)
    TabContainer.Position = UDim2.new(0, 8, 0, 36)
    TabContainer.BackgroundTransparency = 1
    TabContainer.ScrollBarThickness = 2
    TabContainer.ScrollBarImageColor3 = Color3.fromRGB(150, 150, 150)
    TabContainer.Parent = MainFrame

    local TabListLayout = Instance.new("UIListLayout", TabContainer)
    TabListLayout.SortOrder = Enum.SortOrder.LayoutOrder
    TabListLayout.Padding = UDim.new(0, 4)

    -- 右侧页面容器（浅灰半透明背景，与白色区分）
    local PageContainer = Instance.new("Frame")
    PageContainer.Name = "PageContainer"
    PageContainer.Size = UDim2.new(1, -130, 1, -40)
    PageContainer.Position = UDim2.new(0, 126, 0, 36)
    PageContainer.BackgroundColor3 = Color3.fromRGB(240, 240, 240)
    PageContainer.BackgroundTransparency = 0.5
    PageContainer.Parent = MainFrame

    local PageCorner = Instance.new("UICorner", PageContainer)
    PageCorner.CornerRadius = UDim.new(0, 6)

    UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.KeyCode == Enum.KeyCode.RightControl then
            ToggleUI()
        end
    end)

    local WindowObj = {}
    local FirstTab = true

    function WindowObj:Tab(TabConfig)
        local TabTitle = TabConfig.Title or "Tab"
        -- 左侧按钮：白色背景，黑色文字，浅灰边框
        local TabBtn = Instance.new("TextButton")
        TabBtn.Size = UDim2.new(1, -5, 0, 28)
        TabBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        TabBtn.BackgroundTransparency = 0
        TabBtn.Text = TabTitle
        TabBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
        TabBtn.Font = Enum.Font.Gotham
        TabBtn.TextSize = 13
        TabBtn.Parent = TabContainer

        local TabCorner = Instance.new("UICorner", TabBtn)
        TabCorner.CornerRadius = UDim.new(0, 5)
        local TabStroke = Instance.new("UIStroke", TabBtn)
        TabStroke.Color = Color3.fromRGB(200, 200, 200)
        TabStroke.Thickness = 1

        local Page = Instance.new("ScrollingFrame")
        Page.Name = TabTitle .. "_Page"
        Page.Size = UDim2.new(1, -10, 1, -10)
        Page.Position = UDim2.new(0, 5, 0, 5)
        Page.BackgroundTransparency = 1
        Page.ScrollBarThickness = 3
        Page.ScrollBarImageColor3 = Color3.fromRGB(150, 150, 150)
        Page.Visible = false
        Page.Parent = PageContainer

        local PageLayout = Instance.new("UIListLayout", Page)
        PageLayout.SortOrder = Enum.SortOrder.LayoutOrder
        PageLayout.Padding = UDim.new(0, 5)

        PageLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
            Page.CanvasSize = UDim2.new(0, 0, 0, PageLayout.AbsoluteContentSize.Y + 8)
        end)

        local function Activate()
            for _, v in pairs(PageContainer:GetChildren()) do if v:IsA("ScrollingFrame") then v.Visible = false end end
            for _, v in pairs(TabContainer:GetChildren()) do if v:IsA("TextButton") then
                TweenService:Create(v, TweenInfo.new(0.2), {
                    BackgroundColor3 = Color3.fromRGB(255, 255, 255),
                    BackgroundTransparency = 0,
                    TextColor3 = Color3.fromRGB(0, 0, 0)
                }):Play()
            end end
            Page.Visible = true
            -- 激活按钮高亮：浅灰色背景，黑色文字
            TweenService:Create(TabBtn, TweenInfo.new(0.2), {
                BackgroundColor3 = Color3.fromRGB(220, 220, 220),
                BackgroundTransparency = 0,
                TextColor3 = Color3.fromRGB(0, 0, 0)
            }):Play()
        end

        TabBtn.MouseButton1Click:Connect(Activate)
        if FirstTab then Activate() FirstTab = false end

        local TabObj = {}

        -- 按钮组件（浅灰底，黑字）
        function TabObj:Button(BtnConfig)
            local Title = BtnConfig.Title or "Button"
            local Callback = BtnConfig.Callback or function() end
            local BtnFrame = Instance.new("Frame")
            BtnFrame.Size = UDim2.new(1, -5, 0, 28)
            BtnFrame.BackgroundColor3 = Color3.fromRGB(245, 245, 245)
            BtnFrame.BackgroundTransparency = 0
            BtnFrame.Parent = Page
            Instance.new("UICorner", BtnFrame).CornerRadius = UDim.new(0, 5)
            local stroke = Instance.new("UIStroke", BtnFrame)
            stroke.Color = Color3.fromRGB(200, 200, 200)
            stroke.Thickness = 1

            local InteractBtn = Instance.new("TextButton")
            InteractBtn.Size = UDim2.new(1, 0, 1, 0)
            InteractBtn.BackgroundTransparency = 1
            InteractBtn.Text = Title
            InteractBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
            InteractBtn.Font = Enum.Font.Gotham
            InteractBtn.TextSize = 13
            InteractBtn.Parent = BtnFrame

            InteractBtn.MouseButton1Click:Connect(function()
                local s, e = pcall(Callback)
                if not s then warn("Btn Error:"..e) end
                TweenService:Create(BtnFrame, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(200, 200, 200)}):Play()
                task.wait(0.1)
                TweenService:Create(BtnFrame, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(245, 245, 245)}):Play()
            end)
        end

        -- 输入框组件（白色背景，黑色文字）
        function TabObj:TextBox(TxtConfig)
            local Title = TxtConfig.Title or "TextBox"
            local Default = TxtConfig.Default or ""
            local Callback = TxtConfig.Callback or function() end
            local Placeholder = TxtConfig.Placeholder or "请输入数字"

            local TxtFrame = Instance.new("Frame")
            TxtFrame.Size = UDim2.new(1, -5, 0, 32)
            TxtFrame.BackgroundColor3 = Color3.fromRGB(245, 245, 245)
            TxtFrame.BackgroundTransparency = 0
            TxtFrame.Parent = Page
            Instance.new("UICorner", TxtFrame).CornerRadius = UDim.new(0, 5)
            local stroke = Instance.new("UIStroke", TxtFrame)
            stroke.Color = Color3.fromRGB(200, 200, 200)
            stroke.Thickness = 1

            local Label = Instance.new("TextLabel")
            Label.Size = UDim2.new(0, 80, 1, 0)
            Label.Position = UDim2.new(0, 8, 0, 0)
            Label.BackgroundTransparency = 1
            Label.Text = Title
            Label.TextColor3 = Color3.fromRGB(0, 0, 0)
            Label.Font = Enum.Font.Gotham
            Label.TextSize = 13
            Label.TextXAlignment = Enum.TextXAlignment.Left
            Label.Parent = TxtFrame

            local InputBox = Instance.new("TextBox")
            InputBox.Size = UDim2.new(1, -95, 1, -4)
            InputBox.Position = UDim2.new(0, 90, 0, 2)
            InputBox.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
            InputBox.BackgroundTransparency = 0
            InputBox.Text = tostring(Default)
            InputBox.PlaceholderText = Placeholder
            InputBox.TextColor3 = Color3.fromRGB(0, 0, 0)
            InputBox.Font = Enum.Font.Gotham
            InputBox.TextSize = 13
            InputBox.ClearTextOnFocus = false
            InputBox.Parent = TxtFrame
            Instance.new("UICorner", InputBox).CornerRadius = UDim.new(0, 4)
            local InputStroke = Instance.new("UIStroke", InputBox)
            InputStroke.Color = Color3.fromRGB(200, 200, 200)
            InputStroke.Thickness = 1

            InputBox.FocusLost:Connect(function(enterPressed)
                if enterPressed then
                    local val = tonumber(InputBox.Text)
                    if val then
                        Callback(val)
                        Library:Notify("设置成功", Title.."已改为："..val)
                    else
                        Library:Notify("错误", "请输入有效数字")
                        InputBox.Text = tostring(Default)
                    end
                end
            end)
        end

        function TabObj:Label(text)
            local lbl = Instance.new("TextLabel")
            lbl.Size = UDim2.new(1, -10, 0, 24)
            lbl.BackgroundTransparency = 1
            lbl.Text = text
            lbl.TextColor3 = Color3.fromRGB(0, 0, 0)
            lbl.TextSize = 13
            lbl.Font = Enum.Font.Gotham
            lbl.TextXAlignment = Enum.TextXAlignment.Left
            lbl.Parent = Page
        endLibrary:Notify("加载成功", "67 脚本已启动")
        return TabObj
    end
    return WindowObj
end

-- ==================== 脚本功能（输入框版） ====================
local Window = Library:CreateWindow({ Title = "67 脚本中心" })

Library:Notify("加载成功", "67   脚本已启动")

-- 用户信息
local tab_user = Window:Tab({ Title = "用户信息" })
tab_user:Label("玩家: " .. LocalPlayer.Name)
tab_user:Label("注入器: " .. r78_1)
tab_user:Label("脚本免费 禁止倒卖")
tab_user:Button({ Title = "复制QQ群", Callback = function() setclipboard("1097885894") Library:Notify("已复制","1097885894") end })

-- 通用脚本
local tab_common = Window:Tab({ Title = "通用脚本" })
tab_common:Button({ Title = "飞行", Callback = function() loadstring(game:HttpGet("https://pastebin.com/raw/gqv7PXAa"))() end })
tab_common:Button({ Title = "无敌少侠飞行  r6", Callback = function() loadstring(game:HttpGet("https://raw.githubusercontent.com/ke9460394-dot/ugik/refs/heads/main/%E6%97%A0%E6%95%8C%E5%B0%91%E4%BE%A0%E9%A3%9E%E8%A1%8Cr6.txt"))() end })

-- 速度/跳跃/重力 输入框
tab_common:TextBox({
    Title = "移动速度",
    Default = 32,
    Placeholder = "输入16-200",
    Callback = function(val)
        if LocalPlayer.Character then
            LocalPlayer.Character.Humanoid.WalkSpeed = val
        end
    end
})

tab_common:TextBox({
    Title = "跳跃高度",
    Default = 120,
    Placeholder = "输入30-200",
    Callback = function(val)
        local char = LocalPlayer.Character
        if char then
            local hum = char.Humanoid
            hum.UseJumpPower = true
            hum.JumpPower = val
        end
    end
})

tab_common:TextBox({
    Title = "重力",
    Default = 196,
    Placeholder = "输入0-500",
    Callback = function(val)
        game.Workspace.Gravity = val
    end
})

-- 甩飞
local tab_fling = Window:Tab({ Title = "甩飞" })
tab_fling:Label("功能制作中...")

-- 自然灾害生存
local tab_natural = Window:Tab({ Title = "自然灾害生存" })
tab_natural:Button({ Title = "动作脚本", Callback = function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-7yd7-I-Emote-Script-48024"))() end })
tab_natural:Label("仅自然灾害可用")
tab_natural:Button({ Title = "踢人", Callback = function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Fe-DropKick-Script-165813"))() end })
tab_natural:Label("踢完直接踹飞")
